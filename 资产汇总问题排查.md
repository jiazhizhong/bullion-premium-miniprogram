# 资产汇总数据显示问题排查

## 问题描述
资产首页的黄金、白银汇总信息不显示或显示不正确。

## 排查步骤

### 1. 检查接口返回数据

打开微信开发者工具的**控制台**，查看以下日志：

```
接口返回的资产数据: {...}
接口返回的行情数据: {...}
黄金资产列表: [...]
白银资产列表: [...]
```

**预期结果**：
- 资产数据应该包含 `gold` 和 `silver` 两个数组
- 每个资产对象应该包含：`id`, `type`, `price`, `weight`, `channel`, `date`

**如果数据为空**：
- 检查后端服务器是否正常运行
- 检查网络请求是否成功
- 查看 Network 面板中的请求和响应

### 2. 检查数据计算过程

查看控制台中的计算日志：

```
开始计算统计，当前资产数据: {...}
当前行情数据: {...}
计算gold统计，资产数量: X
资产列表: [...]
当前gold价格: XXX.XX
资产1: 价格=XXX, 重量=XXX, 成本=XXX
...
gold统计结果: {...}
```

**预期结果**：
- 资产数量应该大于 0（如果有资产）
- 每个资产的价格和重量应该是数字
- 统计结果应该包含：`totalWeight`, `totalValue`, `avgPrice`, `profit`

### 3. 检查页面数据更新

查看控制台中的更新日志：

```
计算后的黄金统计: {...}
计算后的白银统计: {...}
页面数据已更新
```

**预期结果**：
- 统计结果应该是有效的数字对象
- 所有字段都不应该是 `NaN` 或 `undefined`

### 4. 常见问题及解决方案

#### 问题1: 接口返回数据为空

**症状**：控制台显示 `资产数据为空，使用本地存储数据`

**解决方案**：
1. 确认后端服务器已启动：`cd node-server-mysql && npm start`
2. 检查 API 基础地址是否正确：`miniprogram/utils/api.js` 中的 `BASE_URL`
3. 检查微信开发者工具是否勾选了「不校验合法域名」

#### 问题2: 资产列表为空数组

**症状**：`黄金资产列表: []` 或 `白银资产列表: []`

**解决方案**：
1. 检查接口是否返回了数据
2. 如果接口有数据但前端显示为空，可能是数据结构不匹配
3. 尝试添加一个测试资产，看是否能正常显示

#### 问题3: 统计结果为 0

**症状**：所有统计值都是 0.00

**可能原因**：
1. 资产列表为空
2. 资产数据中的 `price` 或 `weight` 字段为空或无效
3. 行情数据未加载，导致当前价格为 0

**解决方案**：
1. 检查资产数据是否包含有效的 `price` 和 `weight`
2. 检查行情数据是否正常加载
3. 查看控制台日志，确认计算过程

#### 问题4: 页面显示 NaN 或 undefined

**症状**：页面显示 `NaN` 或空白

**解决方案**：
1. 检查 WXML 中的条件判断是否正确
2. 确保所有数值都经过 `parseFloat` 处理
3. 使用 `|| 0` 提供默认值

## 测试数据

接口默认返回的测试数据：

**黄金资产**：
- 资产1: 价格 485.20, 重量 500.00
- 资产2: 价格 480.50, 重量 2043.50
- 总计: 重量 2543.50, 成本约 1,234,567.75

**白银资产**：
- 资产1: 价格 6.82, 重量 5000.00
- 资产2: 价格 6.50, 重量 13234.50
- 总计: 重量 18234.50, 成本约 118,624.45

## 调试命令

在微信开发者工具中：
1. 打开「调试器」面板
2. 切换到「Console」标签
3. 刷新页面，查看所有日志输出
4. 在「Network」标签中查看网络请求

## 下一步

如果问题仍然存在，请提供：
1. 控制台的完整日志输出
2. Network 面板中的请求和响应数据
3. 页面显示的具体错误信息
