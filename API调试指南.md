# API 调试指南

## 一、准备工作

### 1. 启动后端服务器

```bash
cd node-server-mysql
npm start
# 或使用开发模式（自动重启）
npm run dev
```

服务器默认运行在：`http://localhost:3009`

### 2. 配置小程序网络请求

#### 方法一：开发环境（推荐）

在微信开发者工具中：
1. 点击右上角「详情」
2. 在「本地设置」中勾选「不校验合法域名、web-view（业务域名）、TLS 版本以及 HTTPS 证书」
3. 这样可以使用 `http://localhost:3009` 进行本地调试

#### 方法二：使用内网穿透（真机调试）

如果需要真机调试，可以使用内网穿透工具：
- **ngrok**: `ngrok http 3009`
- **localtunnel**: `lt --port 3009`
- **花生壳**: 配置内网穿透

然后将 `utils/api.js` 中的 `BASE_URL` 修改为穿透后的地址。

#### 方法三：配置合法域名（生产环境）

1. 在微信公众平台配置服务器域名
2. 将 `BASE_URL` 修改为配置的域名

## 二、API 配置

### 修改 API 基础地址

编辑 `miniprogram/utils/api.js`：

```javascript
// 开发环境（本地）
const BASE_URL = 'http://localhost:3009/api';

// 或使用内网穿透地址
// const BASE_URL = 'https://your-tunnel-url.com/api';

// 或生产环境地址
// const BASE_URL = 'https://your-domain.com/api';
```

## 三、测试接口

### 1. 测试实时行情接口

在首页（index）加载时，会自动调用：
- `GET /api/market/realtime`

**测试方法**：
1. 打开小程序首页
2. 查看控制台日志
3. 检查价格卡片是否显示数据

### 2. 测试资产接口

**获取资产列表**：
- 打开「资产」页面，会自动调用 `GET /api/assets`

**添加资产**：
- 打开「添加资产」页面
- 填写信息并提交
- 调用 `POST /api/assets`

**删除资产**：
- 在资产列表中点击删除
- 调用 `DELETE /api/assets/:id`

### 3. 测试价格换算接口

- 打开「价格换算」页面
- 输入国际价格（美元/盎司）
- 自动调用 `POST /api/convert` 进行换算

### 4. 测试溢价计算接口

- 打开「溢价信息」页面
- 填写基准价格和买入价格
- 点击计算，调用 `POST /api/premium/calculate`

## 四、调试技巧

### 1. 查看网络请求

在微信开发者工具中：
1. 打开「调试器」面板
2. 切换到「Network」标签
3. 查看所有网络请求和响应

### 2. 查看控制台日志

所有 API 调用都有 `console.error` 输出错误信息：
```javascript
console.error('加载行情数据失败:', error);
```

### 3. 使用断点调试

在 `utils/api.js` 的 `request` 方法中添加断点：
```javascript
function request(options) {
  console.log('请求URL:', BASE_URL + options.url);
  console.log('请求参数:', options.data);
  // ... 添加断点
}
```

### 4. 测试错误处理

可以临时停止后端服务器，测试错误处理逻辑：
- 网络请求失败时的提示
- 数据加载失败时的降级处理

## 五、常见问题

### 1. 网络请求失败

**问题**：提示「网络请求失败，请检查网络连接」

**解决方案**：
- 确认后端服务器已启动
- 检查 `BASE_URL` 配置是否正确
- 检查微信开发者工具的「不校验合法域名」是否已勾选
- 检查防火墙是否阻止了连接

### 2. 跨域问题

**问题**：真机调试时出现跨域错误

**解决方案**：
- 使用内网穿透工具
- 或配置合法域名
- 后端已配置 CORS，确保请求头正确

### 3. 数据格式不匹配

**问题**：接口返回数据但页面不显示

**解决方案**：
- 检查控制台日志，查看返回的数据结构
- 对比 `API文档.md` 中的响应格式
- 检查页面代码中的数据绑定

### 4. 接口返回错误码

**问题**：接口返回 `ec: 400` 或其他错误码

**解决方案**：
- 查看错误消息 `em` 字段
- 检查请求参数是否正确
- 查看后端服务器日志

## 六、调试清单

- [ ] 后端服务器已启动
- [ ] API 基础地址配置正确
- [ ] 微信开发者工具已勾选「不校验合法域名」
- [ ] 首页能正常加载行情数据
- [ ] 资产页面能正常加载资产列表
- [ ] 添加资产功能正常
- [ ] 删除资产功能正常
- [ ] 价格换算功能正常
- [ ] 溢价计算功能正常
- [ ] 错误提示正常显示

## 七、性能优化建议

1. **减少请求频率**：实时行情每3秒更新一次，可根据需要调整
2. **缓存数据**：使用本地存储缓存资产数据，减少请求
3. **错误重试**：网络失败时自动重试（可后续添加）
4. **请求合并**：多个接口可以合并为一个（可后续优化）

## 八、下一步

- [ ] 添加请求重试机制
- [ ] 添加请求缓存
- [ ] 添加请求拦截器（统一处理 token 等）
- [ ] 优化错误提示
- [ ] 添加加载状态指示器
